import { describe, it, expect } from 'vitest';

describe('natural-language parser', () => {
  it('has a placeholder suite until parser cases are added', () => {
    expect(true).toBe(true);
  });
});
