-- NOTE:
-- This seed was replaced with a safe placeholder because the previous file
-- contained terminal output artifacts and broken SQL tokens.
-- Add curated demo fixtures in follow-up migrations/seeds.

begin;
select 'demo_seed_placeholder' as seed_status;
commit;
